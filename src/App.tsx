import React from 'react';
import { IncCounter } from './IncCounter';
import { DecCounter } from './DecCounter';

export function App() {
    return (
        <div>
            <IncCounter />
            <DecCounter />
        </div>
    )
}